spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/thomas
    username: thomas
    password: 'password'
    hikari:
      maximum-pool-size: 30

server:
  error:
    include-binding-errors: always
    include-message: always
